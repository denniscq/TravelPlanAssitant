# Tasks: Simplified Map 力导向布局重构

## 1. 力导向算法骨架

- [ ] 1.1 新建 `src/components/map/forceLayout.ts`，定义入参类型（节点数组、边数组、画布宽高、迭代次数）
- [ ] 1.2 实现 `computeForceLayout` 纯函数：初始化节点位置（随机分布或环形分布），实现单步力计算
- [ ] 1.3 实现三种力：弹簧力（按真实地理距离缩放理想边长）、排斥力（反比平方）、向心力（指向画布中心）
- [ ] 1.4 实现起点终点钉死约束：每轮迭代后把起点重置到左 1/3、终点重置到右 1/3
- [ ] 1.5 实现步长冷却（8.0 → 0.1）和 300 轮迭代主循环
- [ ] 1.6 实现最终归一化：所有节点坐标缩放到画布内边距 30 px 区域内
- [ ] 1.7 实现确定性 wobble：基于节点 id 的 hash 函数，输出 ±2 px 偏移

## 2. 力导向算法单元测试

- [ ] 2.1 新建 `src/components/map/SimplifiedMap.test.ts`
- [ ] 2.2 测试确定性：相同输入两次运行，所有节点坐标字节相同
- [ ] 2.3 测试钉死锚点：起点坐标 ≈ 左 1/3，终点坐标 ≈ 右 1/3
- [ ] 2.4 测试最小间距：任意两节点像素距离 ≥ 100 px
- [ ] 2.5 测试距离排序：构造 3 个节点（A、B 近，B、C 远，A、C 最远），验证 A-B 视觉距离 < A-C 视觉距离
- [ ] 2.6 测试空输入和单节点：返回空布局或单点布局，不抛错
- [ ] 2.7 运行 `npm test` 确保全部通过

## 3. SimplifiedMap 组件重写

- [ ] 3.1 读取 `forceLayout.ts` 结果，组装节点 + 边 + 段号徽章数据
- [ ] 3.2 渲染 SVG 画布：暖米白背景、2 px 深色描边、硬阴影
- [ ] 3.3 渲染节点：圆形 r = 14，2 px 描边，唯一颜色，居中白色序号
- [ ] 3.4 渲染便签纸标签：2 px 描边、白底、硬阴影、±1° 微旋转，名字 + 时间副标签
- [ ] 3.5 渲染线段折线：按交通方式上色，stroke-width 2.5 px，圆角连接
- [ ] 3.6 渲染段号徽章：实心圆 r = 10，交通色填充，居中白色数字，永不旋转
- [ ] 3.7 渲染段信息标签：折线中点旁便签矩形，显示 `距离 · 耗时`，永不旋转
- [ ] 3.8 实现标签防碰撞：4 方向候选位置 + 已占用矩形表，重叠回退到最佳可用
- [ ] 3.9 处理起/终点紫色标记和餐厅橙色标记的差异化渲染
- [ ] 3.10 用 useMemo 缓存 forceLayout 计算，仅当 stops/segments 引用变化时重算

## 4. 视觉与集成验证

- [ ] 4.1 启动 `npm run dev`，手工触发一次完整行程流程，截图保存
- [ ] 4.2 对比新旧实现：核对 8 条验收标准
  - 序号居中节点圆心内
  - 段号徽章在折线几何中点且不旋转
  - 无漂浮节点
  - 起点终点位置正确
  - 远近关系视觉可读
  - 标签水平不重叠
  - 视觉与 `pencil-*` 一致
  - 两次刷新布局完全一致
- [ ] 4.3 跑 `npm run build` 确认编译通过
- [ ] 4.4 跑 `npm test` 确认全部测试通过
- [ ] 4.5 跑 `npm run lint` 确认无新警告

## 5. 文档与收尾

- [ ] 5.1 更新 `.superpowers-memory/CURRENT_STATE.md`：记录本次重构完成
- [ ] 5.2 新增 session journal 条目：本次会话要点
- [ ] 5.3 归档 OpenSpec change：`openspec archive simplified-map-force-layout`
